---
title: Devices Knowledge Base
---
<ul>
<% @items.select { |i| i.identifier.start_with?(@item.identifier) }.each do |section| %>
  <% next if section.identifier == @item.identifier %>
  <li><%= link_to(section[:title], section) %></li>
<% end %>
</ul>
